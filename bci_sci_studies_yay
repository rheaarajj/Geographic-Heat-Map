library(tidyverse)
library(plotly)

df <- read_csv("bci_sci_studies_woo - Sheet1.csv")

country_counts <- df %>%
    group_by(Country) %>%
    summarise(Study_Count = n())

fig <- plot_ly(
    data = country_counts,
    type = "choropleth",
    locations = ~Country,
    locationmode = "country names",
    z = ~Study_Count,
    colorscale = "Blues",
    colorbar = list(title = "Study Count")
) %>%
    layout(
        title = "Global Distribution of BCI Spinal Cord Injury Studies"
    )

fig
